# Leave unset to use production hosts (edge.wingify.net / collect.wingify.net).
# Set to send all SDK traffic through a custom host.
# WINGIFY_PROXY_URL=https://your-host.example.com

# Optional: pin the proxy hostname to an IPv4 address when it is not in public DNS.
# WINGIFY_PROXY_IP=1.2.3.4
